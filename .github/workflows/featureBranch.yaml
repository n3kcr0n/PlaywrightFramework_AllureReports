name: Feature Branch Workflow

on:
  push:
    branches:
      - main # Run when a PR targets main (adjust if needed)

jobs:
  e2e-tests:
    name: Test Feature
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [20]

    steps:
      # 🪄 Checkout code
      - name: Checkout repository
        uses: actions/checkout@v4

      # 🧰 Setup Node.js
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}

      # 🧹 Optional cleanup
      - name: Remove package-lock
        run: rm -f package-lock.json

      # 📦 Install dependencies
      - name: Install packages
        run: npm i -D @types/node

      - name: Install dependencies
        run: npm ci

      # 🎭 Install Playwright browsers
      - name: Install Playwright Browsers
        run: npx playwright install --with-deps

      # 🧪 Run Playwright tests and generate reports
      - name: Run Playwright tests
        run: |
          npx playwright test --reporter=allure-playwright,html --output=playwright-report
        env:
          ALLURE_RESULTS_DIR: allure-results

      # 🧾 Get Allure history
      - name: Get Allure history
        uses: actions/checkout@v4
        with:
          ref: gh-pages
          path: gh-pages

      # 🧱 Generate Allure report
      - name: Generate Allure Report
        uses: simple-elf/allure-report-action@master
        id: allure-report
        with:
          allure_results: allure-results
          gh_pages: gh-pages
          allure_report: allure-report
          allure_history: allure-history

      # 🧹 Clean leftover .git folders (fix for EACCES)
      - name: Clean Allure History Git Metadata
        run: |
          if [ -d "./allure-history/.git" ]; then
            echo "Cleaning leftover .git directory..."
            sudo rm -rf ./allure-history/.git
          fi

      # 🚀 Deploy to GitHub Pages
      - name: Deploy report to GitHub Pages
        if: github.event_name == 'push'
        uses: peaceiris/actions-gh-pages@v4
        with:
          personal_token: ${{ secrets.TOKEN }}
          publish_branch: gh-pages
          publish_dir: allure-history
          allow_empty_commit: true

      # 📤 Upload Allure results
      - name: Upload Allure Results
        uses: actions/upload-artifact@v4
        with:
          name: allure-results
          path: allure-results
          if-no-files-found: error
          retention-days: 30

      # 📤 Upload Allure HTML report
      - name: Upload Allure HTML Report
        uses: actions/upload-artifact@v4
        with:
          name: allure-html-report
          path: allure-report
          if-no-files-found: error
          retention-days: 14

      # 🧾 Upload Playwright HTML report for quick access
      - name: Upload Playwright HTML Report
        uses: actions/upload-artifact@v4
        with:
          name: playwright-html-report
          path: playwright-report
          if-no-files-found: error
          retention-days: 7
